# Claude Code 自动激活配置

**自动启动 Memory Agent Evolution 系统**

## 🚀 自动行为

打开此项目时，系统自动：

1. ✅ 初始化所有 4 个 Agent（QA、Code、Debug、Planning）
2. ✅ 加载 `memory_local/` 中的私密记忆
3. ✅ 启动自动内存服务（后台运行）
4. ✅ 监控对话大小并自动总结
5. ✅ 将洞察保存到 L3 永久记忆

## 📝 使用说明

### Python 代码中使用

```python
# 系统会自动加载，你可以这样访问：
from scripts.load_memory import MemoryLoader

# 加载任何 Agent 的记忆
loader = MemoryLoader('memory_local/code_agent')
memories = loader.load_store(mode='full')

# 查看历史
print(f"✓ 已学习 {len(memories.get('L2', []))} 个代码模式")
print(f"✓ {len(memories.get('L3', []))} 个长期洞察")
```

### 自动总结对话

```python
from scripts.auto_summary import AutoSummarizer

summarizer = AutoSummarizer('memory_local')
result = summarizer.monitor_context(your_conversation)

if result['should_summarize']:
    summary = summarizer.summarize_conversation(your_conversation)
    summarizer.save_to_memory(summary, task_id='task-001')
```

### 自动保存洞察

```python
from auto_memory_service import auto_save_memory

# 保存新的学习
auto_save_memory(
    "发现: 在 async 函数中使用 await 时需要检查 try-except",
    agent_name='code_agent',
    layer='L2'  # L1:语义, L2:流程, L3:洞察
)
```

## 🎯 不需要任何手动操作

所有以下事情都**自动**进行：

- ❌ 不需要手动启动脚本
- ❌ 不需要手动配置参数
- ❌ 不需要手动保存记忆
- ❌ 不需要手动管理上下文

**一切都自动化了！** ⚡

## 📂 项目文件

| 文件 | 用途 |
|------|------|
| `memory_local/` | 私密记忆存储（永不上传） 🔒 |
| `config.local.yaml` | 本地参数（可选修改） |
| `quick_start.py` | 启动脚本 |
| `auto_memory_service.py` | 后台自动保存 |
| `.instructions.md` | 这个文件 |
| `docs/` | 完整文档 |

## 🔧 可选：调整参数

编辑 `config.local.yaml` 来调整行为：

```yaml
agents:
  code_agent:
    auto_summary:
      token_threshold: 3000    # 更敏感的总结（默认 5000）
    mode: 'full'               # 总是使用完整记忆
    
  debug_agent:
    auto_summary:
      token_threshold: 2000    # 更频繁地总结错误
```

## 🌟 快速开始

```bash
# 首次运行
python quick_start.py

# 打开任何 Python 文件 - 系统自动激活！
```

## 📚 了解更多

- [简洁快速指南](README.md)
- [完整中文文档](README_CN.md)
- [集成开发指南](AGENT_INTEGRATION_GUIDE.md)
- [文档索引](docs/INDEX.md)

---

**版本**: 1.0 - 自动激活版  
**更新**: 2026-04-25  
**状态**: ✅ 生产就绪
